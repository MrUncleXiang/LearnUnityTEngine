# TEngine 学习计划与记录

## 1. 目标

这份文档用于：

- 以 `TEngine` 项目为主线，从 0 重新建立知识体系
- 在学习过程中同步记录知识点、理解、问题、验证结果
- 通过“小功能验证 + 提问检查”的方式确认是否真正掌握

学习原则：

1. 先看主线，再补知识点，不一开始钻底层细节
2. 每次只学一小段代码，并立即做验证
3. 学会的标准不是“看过”，而是“能解释、能指出代码位置、能改出结果”

---

## 2. 当前项目事实

- 项目根目录：`D:\E_UnityProject\Learn\TEngine`
- Unity 工程目录：`D:\E_UnityProject\Learn\TEngine\UnityProject`
- 入口场景：`Assets/Scenes/main.unity`
- 主线代码：
  - `Assets/GameScripts/Procedure`
  - `Assets/GameScripts/HotFix/GameLogic`
  - `Assets/TEngine/Runtime/Module`
- 当前项目实际 Unity 版本：`2022.3.62f1c1`
- 当前仓库中的 `AssetRaw/DLL` 目录为空，说明热更产物暂未生成

---

## 3. 学习闭环

每一轮学习都按下面 4 步执行：

1. 看一小段主线代码
2. 补这段代码涉及的知识点
3. 在当前 Demo 上做一个小验证
4. 记录结论，并通过提问或小题确认掌握情况

---

## 4. 学会的判定标准

一个知识点算“已掌握”，至少满足以下 3 条：

- 能用自己的话解释它是什么
- 能指出它在本项目中的代码位置
- 能基于当前 Demo 做出一个小改动或回答一个应用题

---

## 5. 项目主线导图

启动主线：

`main.unity`
-> `ProcedureLaunch`
-> `ProcedureSplash`
-> `ProcedureInitPackage`
-> `ProcedureInitResources`
-> `ProcedurePreload`
-> `ProcedureLoadAssembly`
-> `GameApp`
-> `scene_battle`
-> `BattleSystem`

先看这些文件：

1. `README.md`
2. `UnityProject/Assets/GameScripts/Procedure/ProcedureLaunch.cs`
3. `UnityProject/Assets/GameScripts/Procedure/ProcedureLoadAssembly.cs`
4. `UnityProject/Assets/GameScripts/HotFix/GameLogic/GameApp.cs`
5. `UnityProject/Assets/GameScripts/HotFix/GameLogic/Demo/BattleSystem.cs`

先别深挖这些内容：

- `Assets/TEngine/Editor`
- `Localization` 细节
- `Debugger` 细节
- `HybridCLR` 底层实现
- 全量通读 `TEngine` 所有模块

---

## 6. 知识地图

### 6.1 C# 基础

- 类、对象、字段、属性、方法
- `static`
- 访问修饰符
- `interface`
- 继承与多态
- `enum`
- 泛型
- 委托 / 事件
- `async / await`
- 反射
- 命名空间与程序集

### 6.2 Unity 基础

- Scene
- Prefab
- MonoBehaviour 生命周期
- ScriptableObject
- Canvas / UI
- GameObject 与 Component
- 资源加载
- 协程与异步

### 6.3 框架与工程化

- FSM 状态机
- 模块化设计
- 单例
- 门面模式
- 对象池
- 资源包与资源生命周期
- YooAsset
- HybridCLR

---

## 7. 分阶段学习计划

### 阶段 A：先跑通脑中的主线

目标：

- 知道项目从哪里开始
- 知道启动流程如何切换
- 知道热更业务从哪里进入

学习内容：

1. `README.md`
2. `UnityProject/Assets/Scenes/main.unity`
3. `UnityProject/Assets/TEngine/Settings/ProcedureSetting.asset`
4. `UnityProject/Assets/GameScripts/Procedure/*`
5. `UnityProject/Assets/GameScripts/HotFix/GameLogic/GameApp.cs`

重点知识：

- Unity 项目结构
- ScriptableObject
- FSM 状态机
- C# 基础语法
- 异步流程的基本概念

验证方式：

- 口述启动链
- 画流程图
- 回答流程切换题

### 阶段 B：理解 Demo 是怎么运转的

目标：

- 看懂战斗 Demo 的资源、场景、UI、对象生成
- 知道框架模块如何被业务层调用

学习内容：

1. `UnityProject/Assets/GameScripts/HotFix/GameLogic/GameModule.cs`
2. `UnityProject/Assets/GameScripts/HotFix/GameLogic/Demo/BattleSystem.cs`
3. `UnityProject/Assets/GameScripts/HotFix/GameLogic/Module/UIModule/UIModule.cs`
4. `UnityProject/Assets/GameScripts/HotFix/GameLogic/Demo/PoolManager.cs`
5. `UnityProject/Assets/AssetRaw`

重点知识：

- 单例
- 门面模式
- 对象池
- 事件思想
- Unity 资源与场景

验证方式：

- 给 Demo 加一个小功能
- 能说明一个对象从加载到显示的路径

### 阶段 C：回头理解框架底层模块

目标：

- 理解资源模块、场景模块、RootModule 的职责
- 建立“框架层 -> 热更层 -> Demo 层”的分层认识

学习内容：

1. `UnityProject/Assets/TEngine/Runtime/Module/RootModule.cs`
2. `UnityProject/Assets/TEngine/Runtime/Module/ResourceModule/*`
3. `UnityProject/Assets/TEngine/Runtime/Module/SceneModule/SceneModule.cs`
4. `UnityProject/Assets/TEngine/Runtime/Module/ProcedureModule/*`

重点知识：

- 模块系统
- Driver
- 资源生命周期
- YooAsset 工作流
- HybridCLR 的项目接入方式

验证方式：

- 回答分层设计题
- 能说明某一模块为什么单独存在

---

## 8. 10 天执行版

### Day 1

主题：认识项目全貌

看：

- `README.md`
- `UnityProject/Assets/Scenes/main.unity`
- `UnityProject/Assets/TEngine/Settings/ProcedureSetting.asset`

学：

- Unity 项目目录结构
- Scene
- Prefab
- ScriptableObject

输出：

- 写出项目分层
- 写出启动入口
- 画主启动流程

### Day 2

主题：C# 基础语法在项目中的样子

看：

- `UnityProject/Assets/GameScripts/HotFix/GameLogic/GameApp.cs`
- `UnityProject/Assets/GameScripts/HotFix/GameLogic/GameModule.cs`

学：

- 类、字段、属性、方法
- `static`
- `interface`
- `partial`
- 命名空间

输出：

- 区分 `GameApp` 和 `GameModule` 的职责

### Day 3

主题：FSM 启动流程

看：

- `ProcedureLaunch.cs`
- `ProcedureSplash.cs`
- `ProcedureInitPackage.cs`

学：

- FSM
- 状态切换
- 单一职责

输出：

- 用自己的话解释 3 个流程状态的职责

### Day 4

主题：异步和 UniTask

看：

- `ProcedureInitResources.cs`
- `ProcedureLoadAssembly.cs`

学：

- `async / await`
- 回调
- UniTask
- Coroutine 与 UniTask 的差异

输出：

- 说明为什么资源初始化和 DLL 加载适合异步

### Day 5

主题：资源系统

看：

- `ResourceModuleDriver.cs`
- `UpdateSetting.asset`
- `ProcedureCreateDownloader.cs`

学：

- YooAsset 基本概念
- 包、版本、清单、下载器
- 离线模式和联机模式

输出：

- 总结资源更新主线

### Day 6

主题：热更与程序集

看：

- `GameLogic.asmdef`
- `GameProto.asmdef`
- `ProcedureLoadAssembly.cs`

学：

- 程序集
- DLL
- 反射
- HybridCLR 的作用

输出：

- 解释 `ProcedureLoadAssembly -> GameApp.Entrance`

### Day 7

主题：框架分层与模块化

看：

- `RootModule.cs`
- `SceneModule.cs`
- `BattleSystem.cs`

学：

- 模块化
- 单例
- 门面模式

输出：

- 画出框架层、热更层、Demo 层关系

### Day 8

主题：UI 系统

看：

- `UIModule.cs`
- `BattleMainUI.cs`
- `LoginUI.cs`

学：

- Canvas
- UI 生命周期
- 窗口管理

输出：

- 说清一个 UI 从请求显示到真正显示发生了什么

### Day 9

主题：对象池与事件

看：

- `BattleSystem.cs`
- `PoolManager.cs`

学：

- 对象池
- 事件思想
- GameObject 生命周期

输出：

- 解释为什么这里不用频繁 `Instantiate/Destroy`

### Day 10

主题：配置与综合验证

看：

- `AssetRaw/Configs`
- `BattleSystem.cs`

学：

- 配置驱动
- 数据与逻辑分离

输出：

- 找出哪些参数应该配置化

---

## 9. 功能验证池

后续每完成一个知识点，可以从这里挑一个最小验证任务：

1. 给启动流程加一个简单延时或跳过逻辑
2. 给 Demo 增加一个简单 UI 文本
3. 给战斗场景增加一个新的敌人或子弹
4. 给分数变化增加提示
5. 把敌人数量改成可配置
6. 给资源加载进度增加更清晰的显示

---

## 10. 笔记模板

每次学习记录只写这几项：

### 学习主题

### 看了哪些文件

### 学到的知识点

### 我自己的理解

### 我还不懂的地方

### 我做了什么验证

### 提问检查结果

---

## 11. 掌握检查记录

说明：

- 这里由我在后续学习中持续补充
- 检查形式可以是提问、简答、小题、代码阅读题、小功能任务
- 判定结果分为：`已掌握 / 部分掌握 / 待复习`

### 第 1 轮检查

状态：已完成（部分掌握）

检查范围：

- 项目入口
- 启动流程
- Scene / Prefab / ScriptableObject 基本认识

结果记录：

- Q1 Unity 工程目录：回答正确
- Q2 入口场景：回答正确
- Q3 `ProcedureSetting.asset` 作用：暂未掌握
- Q4 `main.unity`、`ProcedureSetting.asset`、`ProcedureLaunch.cs` 的关系：部分掌握

当前判断：

- 已掌握：仓库根目录与 Unity 工程目录区别、入口场景
- 部分掌握：场景入口和流程入口的区别
- 待补强：`GameEntry -> Settings -> ProcedureSetting -> ProcedureLaunch` 这条调用链

---

## 12. 学习记录

### 2026-06-24 初始化记录

当前状态：

- 已完成项目静态梳理
- 已建立学习计划与记录文档
- 下一步适合正式进入 Day 1

当前已知重点：

- 这是一个 `TEngine + YooAsset + HybridCLR + UniTask` 的 Unity 框架工程
- 入口场景是 `Assets/Scenes/main.unity`
- 启动主线由 `Procedure` 系列状态组成
- 热更业务入口是 `GameApp`
- 当前仓库中的热更 DLL 产物尚未生成

### 2026-06-24 Day 1 进行中

今日主题：

- 认识项目全貌
- 找到入口
- 建立启动主线的第一版理解

今日目标：

- 知道项目根目录和 Unity 工程目录的区别
- 知道入口场景是什么
- 知道 `ProcedureSetting.asset` 的作用
- 能口述启动主线的前半段

今日检查题：

1. 这个仓库的 Unity 工程目录是哪一个？
2. 项目的入口场景是哪一个？
3. `ProcedureSetting.asset` 在这个项目里是做什么的？
4. `main.unity`、`ProcedureSetting.asset`、`ProcedureLaunch.cs` 这三个对象之间是什么关系？

当前状态：

- 已开始 Day 1
- 已完成第一轮检查
- 下一步：补齐 `ProcedureSetting.asset` 的实际调用链理解

### 2026-06-24 Day 1 第二轮反馈

学习者当前表现：

- 已经能主动区分“场景文件”和“其他资源文件”
- 已经能观察到 `GameEntry.Awake()` 在做初始化和流程启动
- 已经初步意识到 `Settings.ProcedureSetting` 通过 `static -> instance` 访问到具体资源

当前主要卡点：

1. 不清楚 `.asset` 是什么
2. 不清楚 `ScriptableObject` 在 Unity 中扮演什么角色
3. 不清楚“场景入口”和“流程入口”的区别
4. 不清楚如果要换启动流程，应该改配置还是改场景

当前判断：

- 已掌握：`GameEntry` 是启动脚本入口之一
- 部分掌握：`Settings -> ProcedureSetting` 的访问链
- 待复习：`ScriptableObject`、`.asset`、流程入口配置化思想

### 2026-06-24 Day 1 第三轮检查结果

检查题：

1. `.asset` 在 Unity 里怎么理解
2. `ScriptableObject` 和 `MonoBehaviour` 的核心区别
3. 为什么入口流程放在 `ProcedureSetting.asset` 里，而不是写死在 `GameEntry.cs`

学习者回答情况：

- Q1：基本正确，已理解为基于 `ScriptableObject` 的资源实例，但对 `.asset` 作为 Unity 通用对象资源容器的理解还可继续加强
- Q2：基本正确，已理解 `MonoBehaviour` 主要依附场景对象、`ScriptableObject` 可作为 Project 里的资源对象
- Q3：正确，已经抓到“配置化而非硬编码”的核心目的

当前判断：

- 已掌握：场景入口、启动脚本入口、流程配置化思想
- 部分掌握：`.asset` 与 `ScriptableObject` 的更准确概念
- 下一步适合进入：`ProcedureLaunch -> ProcedureSplash -> ProcedureInitPackage` 的职责理解

### 2026-06-24 Day 1 当前理解进展

学习者已经形成的正确认识：

- 启动流程被拆成多个独立状态脚本
- 每个状态脚本负责自己阶段内的工作
- 状态脚本依托 FSM 架构组织起来
- `OnEnter` 与 `OnUpdate` 在流程推进中承担不同职责

需要继续校准的点：

- `OnUpdate` 不是因为“完成 OnEnter 后自动切换下一流程”
- 更准确地说，是“当前状态每帧都会收到 OnUpdate，只有代码主动调用 `ChangeState<T>()` 才会切换”

### 2026-06-24 Day 1 FSM 基础检查

检查题：

1. `OnEnter` 会不会每帧执行
2. `OnUpdate` 会不会自动切换状态
3. 真正决定状态切换的关键动作是什么

学习者回答情况：

- Q1：基本正确，已理解 `OnEnter` 不是每帧执行，但对“重复进入同一状态时是否再次触发”还有疑问
- Q2：正确，已理解 `OnUpdate` 不会自动切换状态，但对其持续调用时机还需补强
- Q3：正确，已知道关键动作是 `ChangeState<T>()`

当前判断：

- 已掌握：`OnEnter` 与 `OnUpdate` 的职责区分、状态切换需要显式触发
- 待补强：状态驻留期间 `OnUpdate` 的持续执行逻辑、重新进入状态时 `OnEnter` 的触发规则

### 2026-06-24 Day 1 阶段性笔记

今天已建立的理解：

- 仓库根目录是 `TEngine`，真正的 Unity 工程目录是 `UnityProject`
- 入口场景是 `Assets/Scenes/main.unity`
- `GameEntry` 是启动脚本入口
- `ProcedureSetting.asset` 是流程配置入口
- 当前默认入口流程是 `ProcedureLaunch`
- 启动链前半段可以先记成：
  - `main.unity`
  - `GameEntry.Awake()`
  - `Settings.ProcedureSetting.StartProcedure()`
  - `ProcedureLaunch`
  - `ProcedureSplash`
  - `ProcedureInitPackage`

今天已经接触到的知识点：

- Unity 工程目录结构
- Scene
- ScriptableObject
- `.asset`
- FSM 基本概念
- `OnEnter`
- `OnUpdate`
- `ChangeState<T>()`
- 配置化而非硬编码

今天最重要的一句话：

- FSM 不会自动帮你进入下一个状态，是否切换取决于代码有没有主动调用 `ChangeState<T>()`

后续补充记录：

- 已于 `2026-06-25` 创建单独当天笔记：`学习笔记-2026-06-25.md`
- 当天额外学习任务已记录到该笔记中
- 当前优先复习：FSM 基础、`MonoBehaviour` / `ScriptableObject`、`OnEnter` / `OnUpdate` / `ChangeState<T>()`

---

## 13. 后续协作方式

后续建议这样推进：

1. 我带你进入当天学习内容
2. 过程中我会提出 2 到 5 个检查题
3. 你回答后，我会把结果记录到本文件
4. 到合适时机，我们做一个基于当前 Demo 的小功能验证

如果后续需要，也可以继续在这里追加：

- 每日学习记录
- 问答记录
- 复习清单
- 踩坑总结
