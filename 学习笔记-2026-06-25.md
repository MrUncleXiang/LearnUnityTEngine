# 学习笔记 - 2026-06-25

记录时间：`2026-06-25 00:18:20 +08:00`

学习阶段：`Day 1`

学习主题：

- 认识 TEngine 项目结构
- 找到项目入口
- 初步理解流程系统（Procedure + FSM）
- 理解 `ScriptableObject`、`.asset`、`OnEnter`、`OnUpdate`、`ChangeState<T>()`

---

## 1. 今天看了什么

- `README.md`
- `UnityProject/Assets/Scenes/main.unity`
- `UnityProject/Assets/TEngine/Settings/ProcedureSetting.asset`
- `UnityProject/Assets/GameScripts/GameEntry.cs`
- `UnityProject/Assets/TEngine/Runtime/Module/Settings/Settings.cs`
- `UnityProject/Assets/TEngine/Runtime/Module/ProcedureModule/ProcedureSetting.cs`
- `UnityProject/Assets/GameScripts/Procedure/ProcedureLaunch.cs`
- `UnityProject/Assets/GameScripts/Procedure/ProcedureSplash.cs`
- `UnityProject/Assets/GameScripts/Procedure/ProcedureInitPackage.cs`

---

## 2. 今天学到了什么

### 2.1 Unity 工程目录

- 仓库根目录是：`D:\E_UnityProject\Learn\TEngine`
- 真正的 Unity 工程目录是：`D:\E_UnityProject\Learn\TEngine\UnityProject`

我的理解：

- 仓库根目录可以放 README、许可证、学习笔记等
- `UnityProject` 才是 Unity 编辑器真正打开和运行的工程目录

### 2.2 项目入口

- 入口场景是：`Assets/Scenes/main.unity`

我的理解：

- `main.unity` 是场景入口
- 进入这个场景后，场景中的对象和脚本开始工作

### 2.3 `GameEntry` 的作用

我的理解：

- `GameEntry` 是启动脚本入口
- 它在 `Awake()` 中初始化一些模块
- 然后通过 `Settings.ProcedureSetting.StartProcedure()` 启动流程系统

### 2.4 `ScriptableObject` 和 `.asset`

我的当前理解：

- `MonoBehaviour` 是依附在 `GameObject` 上运行的脚本
- `ScriptableObject` 更适合做独立的数据/配置对象
- `.asset` 可以理解成 Unity 保存这类对象资源的文件容器
- `ProcedureSetting.asset` 是一个 `ProcedureSetting : ScriptableObject` 的资源实例

### 2.5 `ProcedureSetting.asset` 的作用

我的理解：

- 它不是场景本身
- 它是一份流程配置对象
- 它定义了：
  - 哪些流程状态可用
  - 哪个流程是入口流程

当前项目中：

- 入口流程是：`Procedure.ProcedureLaunch`

### 2.6 启动调用链

我目前整理出来的主线：

`main.unity`
-> `GameEntry.Awake()`
-> `Settings.ProcedureSetting.StartProcedure()`
-> 读取 `ProcedureSetting.asset`
-> 入口流程 `ProcedureLaunch`

---

## 3. 今天对 FSM 的理解

### 3.1 基本理解

- 启动流程被拆成多个独立状态
- 每个状态脚本只负责当前阶段的一件事
- 状态之间通过 `ChangeState<T>()` 进行切换

### 3.2 我当前对几个函数的理解

- `OnEnter`
  - 每次进入状态时执行一次
- `OnUpdate`
  - 只要当前还停留在这个状态里，就会每帧执行
- `ChangeState<T>()`
  - 主动切换到下一个状态

### 3.3 最重要的一句话

- FSM 不会自动帮我进入下一个状态，是否切换取决于代码有没有主动调用 `ChangeState<T>()`

---

## 4. 今天看到的 3 个流程状态

### 4.1 `ProcedureLaunch`

我目前的理解：

- 负责启动准备
- 初始化启动器 UI
- 初始化语言
- 初始化声音
- 然后切换到 `ProcedureSplash`

### 4.2 `ProcedureSplash`

我目前的理解：

- 当前项目里是一个很轻的过渡状态
- 目前代码里基本是直接切到 `ProcedureInitPackage`

### 4.3 `ProcedureInitPackage`

我目前的理解：

- 负责初始化资源包
- 成功后才会进入 `ProcedureInitResources`
- 失败时不会自动继续，而是弹出提示和重试逻辑

---

## 5. 今天最关键的纠正

我一开始的误区：

- 以为 `OnEnter` 执行完之后会自动进入下一个状态

修正后的理解：

- `OnEnter` 只是进入状态时执行一次
- `OnUpdate` 会在当前状态驻留期间持续每帧执行
- 是否切换，取决于代码是否调用了 `ChangeState<T>()`

---

## 6. 我现在还没完全懂的地方

- `.asset` 更完整、准确的 Unity 定义
- `ScriptableObject` 在项目里还能怎么用
- `ProcedureInitPackage` 中的异步初始化细节
- `_resourceModule` 的来源和完整生命周期

---

## 7. 今天已经掌握的内容

- Unity 工程目录和仓库根目录的区别
- 入口场景位置
- `GameEntry` 是启动脚本入口
- `ProcedureSetting.asset` 是流程配置入口
- `ProcedureLaunch` 是当前默认入口流程
- FSM 中 `OnEnter`、`OnUpdate`、`ChangeState<T>()` 的基本作用

---

## 8. 明天/下一步建议学习什么

优先继续：

1. 巩固 `FSM`
2. 继续理解 `ProcedureLaunch -> ProcedureSplash -> ProcedureInitPackage -> ProcedureInitResources`
3. 补 `ScriptableObject` 和 `.asset`
4. 开始接触 `UniTask / async-await`

暂时不要深入：

- `HybridCLR` 底层
- `YooAsset` 深层细节
- 全量 `TEngine` 模块源码

---

## 9. 今天额外学习任务

今天额外补充的知识点只聚焦当前代码已经遇到的内容，不扩散到更深的框架细节。

### 9.1 FSM 基础概念

需要掌握：

- 什么是状态
- 什么是当前状态
- 什么是状态切换
- 为什么启动流程适合拆成多个状态

对应到本项目：

- `ProcedureLaunch` 是一个状态
- `ProcedureSplash` 是一个状态
- `ProcedureInitPackage` 是一个状态
- `ChangeState<T>()` 是状态切换动作

### 9.2 MonoBehaviour 和 ScriptableObject

需要掌握：

- `MonoBehaviour` 依附在 `GameObject` 上运行
- `ScriptableObject` 更适合作为独立配置对象
- `ProcedureSetting.asset` 是 `ProcedureSetting : ScriptableObject` 的资源实例

### 9.3 OnEnter / OnUpdate / ChangeState

需要掌握：

- `OnEnter`：每次进入状态时执行一次
- `OnUpdate`：停留在当前状态期间每帧执行
- `ChangeState<T>()`：主动切换到下一个状态

### 9.4 今天暂时不深入

- `HybridCLR` 底层
- `YooAsset` 深层原理
- `async / await` 深入机制
- 设计模式大全
- 全套 C# 语法体系

### 9.5 今日练习题

1. 为什么 `ProcedureLaunch`、`ProcedureSplash`、`ProcedureInitPackage` 要拆成不同状态？
2. 为什么 `ProcedureInitPackage` 初始化失败时不会自动进入 `ProcedureInitResources`？
3. 用自己的话解释 `ProcedureSetting.asset` 为什么比把入口流程写死在 `GameEntry.cs` 里更灵活。

---

## 10. 今天我会怎么用一句话概括这个项目

这是一个基于 `TEngine + YooAsset + HybridCLR + UniTask` 的 Unity 框架工程，使用流程状态机组织启动过程，通过配置化的 `ProcedureSetting.asset` 决定从哪个流程状态开始运行。
